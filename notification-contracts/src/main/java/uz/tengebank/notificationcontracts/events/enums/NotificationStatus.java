package uz.tengebank.notificationcontracts.events.enums;

public enum NotificationStatus {
    REQUEST_REJECTED,
    REQUEST_ACCEPTED,
    CHANNEL_ROUTED,
    PROCESSING_STARTED,
    PROVIDER_ACCEPTED,
    PROVIDER_REJECTED,
    ATTEMPT_FAILED,
    DELIVERED,
    UNDELIVERED
}
